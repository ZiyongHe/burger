
<li data-id="{{id}}" class="d-flex align-items-center">
  {{burger_name}}
  {{#if devoured}}
  <button class="delete ml-auto btn">Delete</button>
  {{else}}
  <button class="devour ml-auto btn">Devour</button>
  {{/if}}
</li>